/**
 * Created by tomwendland on 20.05.17.
 */
/*
document.addEventListener("DOMContentLoaded", function () {

    var sliderboxes = content.getElementsByClassName("sliderbox");

    for (var i = 0; i < sliderboxes.length; i++) {
        var sliderbox = sliderboxes[i];

        var title = sliderbox.getAttribute("title");
        var id = sliderbox.getAttribute("id");

        var clone = sliderbox.cloneNode(true);
        clone.classList.remove("new");



        content.innerHTML +=
            '<div class="pic-container">' +
            clone.outerHTML +
            '<div class="pic-buttons">' +
            '<label class="Up">' +
            '<input type="checkbox">' +
            '<img src="ThumbUpOff.png">' +
            '</label>' +
            '<label>' +
            '<input type="checkbox" class="pic-fullsize-button">' +
            '<img src="FullOff.png">' +
            '</label>' +
            '</div>' +
            '</div>';


            <label class="checkbox info inlineSwitch" id="boxautocheckout-div" title="Checkout after delay. Will turned off after usage">
            <label class="switch">
            <input type="checkbox" id="boxautocheckout" >
            <div class="slider"></div>
            </label>
            <label class="info" for="boxautocheckout">AutoCheckout</label>
            </label>


    }

    while (sliderboxes.length > 0) {
        var sliderbox = sliderboxes[0];
        sliderbox.parentNode.removeChild(sliderbox);
    }
}*/